# Use the official Nginx image from Docker Hub
FROM nginx:latest

# Copy your static website (index.html) to Nginx's default location
COPY index.html /usr/share/nginx/html/

# Expose the default Nginx port
EXPOSE 80
